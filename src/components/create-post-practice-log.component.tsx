import React, { Component } from "react";
import { Link } from "react-router-dom";

export default class CreatePostPracticeLog extends Component {
  render() {
    return (
      <div>
        <p>You are on the PostPracticeLog component!</p>
      </div>
    );
  }
}
